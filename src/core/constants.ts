export enum Tab {
    CHEAP = "cheap",
    FAST = "fast",
    OPTIMAL = "optimal",
}
export enum Load {
    MORE = 5
}

export enum Filter {
    ISALL = 'isAll',
    ISZERO = '0',
    ISONE = '1',
    ISTWO = '2',
    ISTHREE = '3'
}